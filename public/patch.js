/**
 * Spectre.io Live Patch - Reset (V1.4)
 * 
 * We have moved to a pure native Android Desktop Browser Engine.
 * All manual CSS layout injection has been removed.
 * NTL will render exactly like it does on a PC.
 */

(function() {
    console.log('Spectre Live Patch V1.4: Pure Browser Engine Active - No CSS tampering.');
    // Letting Android WebView handle all the scaling and zooming natively.
})();
